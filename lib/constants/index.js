'use strict';

const TABLE_AUTH_TOKENS = 'AuthTokens';
const TABLE_USERS = 'Users';

const WHITE_LIST = ['/sign-up', '/sign-in'];

module.exports = {
  TABLE_AUTH_TOKENS,
  TABLE_USERS,
  WHITE_LIST
};
